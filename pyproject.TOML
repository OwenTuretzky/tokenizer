[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tokenizer"
version = "0.1"
description = "A from-scratch tokenizer for Japanese and English"
requires-python = ">=3.14"